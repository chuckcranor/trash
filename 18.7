#yeah, let's do the "MPICH_GNI_NDREG_ENTRIES" comparison experiments, so 
#these will be two vpic 32-node runs with ofi+gni with and without 
#setting the MPICH_GNI_NDREG_ENTRIES.

#!/bin/sh

std="--constraints haswell --experiment sc17bi --iterations 1 \
--overcommit enable --tag rc18-1h-gni-ndreg --tests deltafs --skipreads 
\
--env XX_IGNORE_DIRS=fields:hydro:rundata:names \
--env XX_SH_THREE_HOP=0 \
--env XX_HG_PROTO=ofi+gni \
--env XX_ALWAYS_SHUFFLE=0 \
--env XX_BYPASS_CH=1 \
--env XX_BYPASS_WRITE=1 \
--env XX_SKIP_SAMP=1"

../vpicexpt_gen.pl ${std} --run 6 --dw off .


I am not sure when do we do the export MPICH_GNI_NDREG_ENTRIES=???, 
maybe right before we do the sbatch command?
